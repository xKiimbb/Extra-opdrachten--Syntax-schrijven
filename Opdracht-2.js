// Definieer een functie genaamd drieGetallenOptellen.
// De functie accepteert drie parameters, genaamd x, y en z.
// Geef de optelsom van de drie parameters terug als resultaat.

function drieGetallenOptellen(x, y, z) {
  return x + y + z;
}

console.log(drieGetallenOptellen(1, 2, 3));
